Run ALL_IN_ONE_RUN_ALL_SVG.bat. It compiles C++, generates SVG, then creates two different HTML files:
index.html = step player with speed/autoplay.
auto_generated_index.html = all images gallery, no step player.
C++ files are not modified.
